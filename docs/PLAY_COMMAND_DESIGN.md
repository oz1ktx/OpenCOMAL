# PLAY Command Design (Merged)

This document has been merged into:

- `docs/PLAY_COMMAND.md`

Please update only `PLAY_COMMAND.md` going forward.

## Why

The old split between "design" and "status" caused drift from actual implementation.
Keeping one canonical file makes it easier to track:

- what works now
- what is planned next (including waveform and sample support)
