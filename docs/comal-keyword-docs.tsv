# OpenCOMAL external keyword help docs (tab-separated)
# Format: KEYWORD<TAB>CATEGORY<TAB>DESCRIPTION
# Notes:
# - Lines starting with # are ignored.
# - Use \n for line breaks in descriptions.
# - Entries override built-in hardcoded docs by keyword (case-insensitive).
# - You can also add new keywords or built-in functions not listed in code.

DRAW	Graphics and Sound	Execute a graphics command from the registry.\n\nGeneral form:\nDRAW commandName, arg1, arg2, ...\n\nHow to write commandName:\n1) Bare command: DRAW line, 0, 0, 100, 100\n2) Quoted command: DRAW "line", 0, 0, 100, 100\n3) Group-qualified command: DRAW "HUD.line", 0, 0, 100, 100\n4) Nested groups: DRAW "Ship.Engine.line", 0, 0, 100, 100\n\nGroup notation means everything before the last dot is a group path, and the final token is the command.\nExample: "Ship.Engine.rect" => group path Ship -> Engine, command rect.\n\nSupported commands and signatures:\nline x1 y1 x2 y2\nrect x y width height\ncircle cx cy radius\nellipse x y width height\nstroke r g b [a]\nfill r g b [a]\nnoFill\nnoStroke\nlineWidth width\nbackground r g b [a]\nclear\ntranslate dx dy\nrotate angle\nscale sx [sy]\ntext x y "text"\nfontSize size\n\nTip: hover the subcommand token (including in qualified forms like Ship.rect) to see command-specific help and arity.

PLAY	Graphics and Sound	Play a melody/control sequence string.\n\nForm:\nPLAY sequence$ [, options...]\n\nImplemented runtime options:\n- ASYNC or NOWAIT\n- VOL=<n> or VOL, n\n- DUR=<ms> or DUR, ms\n- Special command: PLAY "STOP"\n\nCurrent ABC subset (via parseABCToTones):\n- Notes: A B C D E F G\n- Accidentals prefix: ^ (sharp), _ (flat), = (natural marker)\n- Rests: z or Z (with lengths, e.g. z2, z/2)\n- Directives: L=<beats> or L:<beats> (default note length), Q=<bpm> or Q:<bpm> (tempo)\n- Octave modifiers:\n  lowercase note => +1 octave\n  comma (,) => -1 octave each\n  apostrophe (') => +1 octave each\n  explicit octave digits, e.g. C5 or C3\n- Length suffixes: A2, A/2, A/, A3/2\n- Bar separators ignored: |, ||, |], [|\n- Comments: % comment text (ignored to end of line)\n- Note ties: - (ignored, not needed for simple playback)\n\nPractical examples:\nPLAY "L=1/2 Q:120 C z2 | D z/2 || E [| F |]"\nPLAY "L=1 Q=120 C D E % major scale F G A B C'"\n\nNote: this is a lightweight subset, not full ABC/MML compatibility.

TONE	Graphics and Sound	Play a sine tone.\n\nForm:\nTONE frequency, durationMs\n\nfrequency is in Hz and duration is in milliseconds.

SPLIT$	Strings	Return one segment from a delimited string.\n\nForm:\nSPLIT$(text$, delimiter$, index)\n\nindex is zero-based in current runtime behavior.

SPAWN	Parallel and Queue	Start a CLOSED PROC asynchronously (fire-and-forget).\n\nForm:\nSPAWN procName\nSPAWN procName(arg1, arg2, ...)\n\nRules in current implementation:\n- Target must be a PROC (not FUNC).\n- Target PROC must be declared CLOSED.\n- Spawned workers are detached from the main flow (no process id / no join).\n- When the main program ends, spawned workers are stopped.\n- A spawned CLOSED PROC can call only CLOSED PROC/FUNC routines.\n- DIM/LOCAL is not allowed inside CLOSED PROC/FUNC in this restrictive mode.
