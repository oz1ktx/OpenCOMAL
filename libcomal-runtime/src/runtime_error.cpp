/// @file runtime_error.cpp
/// ComalError formatting utilities (if needed in the future).
/// Currently the error types are header-only; this file exists to
/// satisfy the CMake SOURCES list and provide a place for any
/// future error-message lookup tables.

#include "comal_error.h"

namespace comal::runtime {

// Reserved for future: error code → human-readable message table.

} // namespace comal::runtime
