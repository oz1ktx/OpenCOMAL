// collatz sequence demo program

PROC collatz(x) CLOSED
  WHILE x>2 DO
    PRINT x,
    IF x MOD 2 = 0 THEN
      x:=x/2
    ELSE
      x:=x*3+1
    ENDIF
  ENDWHILE
  PRINT
ENDPROC

FOR i:=2 TO 10 DO
  spawn collatz(i)
ENDFOR
